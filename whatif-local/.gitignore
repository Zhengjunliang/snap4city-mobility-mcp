# Cloned third-party source (build it locally, don't vendor it into our repo).
whatif-router-src/
# Downloaded data (PBF ~450MB + GTFS) and the graph GraphHopper builds from them.
data/
# Test outputs.
test-output.json
test-output-foot.json
# JupyterHub native run (run-on-jupyterhub.sh): uploaded war + downloaded Tomcat.
whatif-router.war
apache-tomcat-*/
